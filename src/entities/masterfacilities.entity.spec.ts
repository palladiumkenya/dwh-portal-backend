import { MasterfacilitiesEntity } from './masterfacilities.entity';

describe('MasterfacilitiesEntity', () => {
  it('should be defined', () => {
    expect(new MasterfacilitiesEntity()).toBeDefined();
  });
});
